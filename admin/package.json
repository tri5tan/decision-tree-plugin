{
  "name": "ct-decision-tree-admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:admin && npm run build:viewer",
    "build:admin": "vite build --config vite.admin.config.js",
    "build:viewer": "vite build --config vite.viewer.config.js",
    "dev":   "vite"
  },
  "dependencies": {
    "dagre":    "^0.8.5",
    "reactflow": "^11.11.4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "react":                "^18.3.1",
    "react-dom":            "^18.3.1",
    "vite":                 "^6.2.0"
  }
}
